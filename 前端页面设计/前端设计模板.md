# 页面名称：[在此填写页面名称，例如：用户管理列表页]

> **给 AI 的指令**：
>
> 请根据《项目全局技术栈与开发规范》以及下方的页面需求文档，为我编写完整的前端代码。要求逻辑清晰、组件拆分合理，并使用严格的 TypeScript 类型。

## 1. 页面基本信息

- **页面路由 (Route)**：`/users/list`
- **页面角色权限**：[例如：仅限 Admin 和 Manager 访问]
- **核心目标**：[一句话描述该页面的主要作用，例如：支持对系统用户的增删改查操作]

## 2. 页面布局与视觉结构 (Layout & Visuals)

> **提示**：描述页面从上到下、从左到右的区块划分。

本页面分为以下几个核心区域：

1. **顶部操作区 (Toolbar)**：
   - 左侧：搜索输入框 (Placeholder: "请输入用户名")，状态下拉筛选框。
   - 右侧：主色调的“新增用户”按钮。
2. **中间数据区 (Table/List)**：
   - 包含的列名：ID、头像、用户名、角色、状态（Tag标签：正常/禁用）、操作列。
   - 操作列包含：编辑按钮（蓝色）、删除按钮（红色）。
3. **弹窗/抽屉区 (Modal/Drawer)**：
   - 隐藏的表单弹窗，标题根据状态显示为“新增用户”或“编辑用户”。
   - 包含表单项：用户名(Input，必填)、角色(Select)、状态(Switch，默认开启)。

## 3. 数据状态定义 (State Management)

> **提示**：明确告诉 AI 这个页面需要维护哪些核心变量，避免 AI 乱写原生 DOM 操作。

请在组件内部或 Store 中定义以下响应式状态：

- `tableData` (Array): 表格列表数据。
- `isLoading` (Boolean): 表格请求的加载状态，默认 `false`。
- `searchParams` (Object): 包含 `keyword` 和 `status` 的查询参数。
- `modalVisible` (Boolean): 控制弹窗的显示与隐藏。
- `modalType` (String): 弹窗模式，枚举值：`'create'` | `'update'`。
- `currentRecord` (Object | null): 当前正在编辑的行数据，新增模式下为 `null`。

## 4. 交互逻辑与事件 (Interactions & Events)

> **提示**：按照用户的操作路径，描述具体的业务因果关系。

请实现以下交互逻辑：

1. **初始化/查询**：
   - 页面挂载 (Mount) 时，自动调用 `fetchList` 方法。
   - 点击搜索或分页切换时，携带 `searchParams` 重新调用 `fetchList`，并在请求期间开启 `isLoading`。
2. **删除操作**：
   - 点击“删除”按钮时，触发气泡确认框 (Popconfirm)：“确定要删除该数据吗？”。
   - 确认后调用删除 API，成功后弹出全局提示，并刷新当前列表。
3. **表单提交 (新增/编辑)**：
   - 弹窗内点击“提交”：先触发前端表单的规则校验。
   - 校验通过后，根据 `modalType` 调用不同的 API (POST/PUT)。
   - 请求成功后：关闭弹窗 -> 清空表单 -> 弹出成功提示 -> 刷新列表。

## 5. Mock 数据与接口结构 (Data Interface)

> **提示**：提供 DTO 结构，让 AI 提前写好 TS Type 和 Mock 数据，方便直接预览。

- 请为页面定义相关的 TypeScript Interface，例如 `IUserListItem`。

- 如果没有真实的 API，请在代码中生成包含 3~5 条结构一致的 Mock 数组用于界面预览测试。

- 预期的后端返回数据结构示例：

  ```
  {
    "id": 1001,
    "username": "admin_test",
    "role": "ADMIN",
    "status": 1
  }
  ```

## 6. 组件拆分要求 (Component Splitting Rules)

为了保证代码的可维护性，请务必按照以下粒度拆分代码，**绝对不要将所有代码写在同一个大文件里**：

1. 页面主入口 (如 `index.tsx` 或 `index.vue`)：只负责组装组件和管理顶层状态。
2. 搜索过滤区抽离为独立组件。
3. 表单弹窗抽离为独立组件 (内部包含完整的表单校验逻辑)。
